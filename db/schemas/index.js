const userSchema = require("./user");
const chatSchema = require("./chat");
const bookSchema = require("./book");
const postSchema = require("./post");

module.exports = { userSchema, chatSchema, bookSchema, postSchema };
